# Team Collaboration Rules

## 🛑 CRITICAL: Git Sync Protocol
- **BEFORE ANY EDIT**: You MUST run `git fetch` then `git pull`.
- **NEVER** edit a file without confirming you have the latest remote changes.
- **IF CONFLICT**: Stop immediately. Do NOT auto-resolve. Ask the human to resolve.
